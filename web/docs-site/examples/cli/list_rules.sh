# Docs CLI example — print the numbered AGS4 rule catalogue (title / severity / fixable).
# The page shows only the command; the gate byte-compares stdout to list_rules.out.
# expect-exit: 0
# --8<-- [start:cmd]
lat rules
# --8<-- [end:cmd]
