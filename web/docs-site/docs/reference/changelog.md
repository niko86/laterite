# Changelog

Release notes for `laterite` are published as **GitHub Releases**, one per
version tag:

**→ [github.com/niko86/laterite/releases](https://github.com/niko86/laterite/releases)**

Each release lists what changed in that version and links its published
artifacts — the PyPI wheel, the `lat` CLI binaries, and the npm packages.
