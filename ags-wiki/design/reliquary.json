{
  "target": "ags-wiki/design/reliquary.md",
  "marker": "reliquary-register",
  "generator": "tools/gen_wiki_tables.py",
  "fields": [
    {
      "header": "Symbol",
      "key": "symbol"
    },
    {
      "header": "Verify",
      "key": "verify"
    },
    {
      "header": "Axis",
      "key": "axis"
    },
    {
      "header": "Status",
      "key": "status"
    },
    {
      "header": "Removed-in",
      "key": "removed_in"
    },
    {
      "header": "Evidence",
      "key": "evidence"
    }
  ],
  "rows": [
    {
      "symbol": "`truncate_dt_to_unit()`",
      "verify": "`rust-packages/laterite-ags4-types/src/lib.rs::fn truncate_dt_to_unit`",
      "axis": "code",
      "status": "removed",
      "removed_in": "misc-deadcode PR",
      "evidence": "AGS5 DT-precision writer; 0 shipped callers (only ags5/)"
    },
    {
      "symbol": "`LineTerminator::as_bytes()`",
      "verify": "`rust-packages/laterite-ags4-parse/src/lib.rs::as_bytes(self)`",
      "axis": "code",
      "status": "removed",
      "removed_in": "misc-deadcode PR",
      "evidence": "unused ergonomic twin of `as_str()`; 0 call sites"
    },
    {
      "symbol": "`tempfile` (dev-dep)",
      "verify": "`rust-packages/laterite-transport/Cargo.toml::tempfile`",
      "axis": "dependency",
      "status": "removed",
      "removed_in": "dep-hygiene PR",
      "evidence": "declared, never used; tests use `env::temp_dir()`"
    },
    {
      "symbol": "`chrono` (dep)",
      "verify": "`rust-packages/laterite-ags4-core/Cargo.toml::chrono`",
      "axis": "dependency",
      "status": "removed",
      "removed_in": "dep-hygiene PR",
      "evidence": "0 chrono/DateTime uses in core src/tests"
    },
    {
      "symbol": "`CliError::NotImpl`",
      "verify": "`rust-packages/laterite-ags4-core/src/error.rs::NotImpl`",
      "axis": "code",
      "status": "removed",
      "removed_in": "core-relics PR",
      "evidence": "ags5db stub variant; last use retired even in ags5"
    },
    {
      "symbol": "`GroupDescriptor::non_key_headings()`",
      "verify": "`rust-packages/laterite-ags4-core/src/registry.rs::fn non_key_headings`",
      "axis": "code",
      "status": "removed",
      "removed_in": "core-relics PR",
      "evidence": "ergonomic pair of `key_headings()`; 0 shipped callers"
    },
    {
      "symbol": "`Heading::py_name()`",
      "verify": "`rust-packages/laterite-ags4-core/src/registry.rs::fn py_name`",
      "axis": "code",
      "status": "removed",
      "removed_in": "core-relics PR",
      "evidence": "ags5 DuckDB column-alias helper; 0 shipped callers"
    },
    {
      "symbol": "`count_by_group()`",
      "verify": "`rust-packages/laterite-ags4-validator/src/findings.rs::fn count_by_group`",
      "axis": "code",
      "status": "removed",
      "removed_in": "misc-deadcode PR",
      "evidence": "only its own unit test calls it"
    },
    {
      "symbol": "`comfy-table` (dep)",
      "verify": "`rust-packages/laterite-cli/Cargo.toml::comfy-table =`",
      "axis": "dependency",
      "status": "removed",
      "removed_in": "dep-hygiene PR",
      "evidence": "0 direct use; served via `laterite-cliutil`"
    },
    {
      "symbol": "`indicatif` (dep)",
      "verify": "`rust-packages/laterite-cli/Cargo.toml::indicatif =`",
      "axis": "dependency",
      "status": "removed",
      "removed_in": "dep-hygiene PR",
      "evidence": "0 direct use; served via `laterite-cliutil::Spinner`"
    },
    {
      "symbol": "`SelfCheck.ok` field",
      "verify": "`rust-packages/laterite-ags4-parity/src/oracle.rs::pub ok: bool`",
      "axis": "code",
      "status": "removed",
      "removed_in": "misc-deadcode PR",
      "evidence": "hardcoded `true`; never read"
    },
    {
      "symbol": "`rayon` (dep)",
      "verify": "`rust-packages/laterite-ags4-forge/Cargo.toml::rayon`",
      "axis": "dependency",
      "status": "removed",
      "removed_in": "dep-hygiene PR",
      "evidence": "full parallelism crate, never called"
    },
    {
      "symbol": "`registry_get_group()`",
      "verify": "`rust-packages/laterite-py/src/registry_fns.rs::fn registry_get_group`",
      "axis": "code",
      "status": "removed",
      "removed_in": "python-registry PR",
      "evidence": "dead PyO3 export; py loads via bulk JSON"
    },
    {
      "symbol": "`Heading.indexed` field",
      "verify": "`packages/laterite/python/laterite/registry.py::indexed`",
      "axis": "code",
      "status": "removed",
      "removed_in": "python-registry PR",
      "evidence": "hardcoded `None` in registry.rs; never populated"
    },
    {
      "symbol": "`GroupDescriptor.index_parent` field",
      "verify": "`packages/laterite/python/laterite/registry.py::index_parent`",
      "axis": "code",
      "status": "removed",
      "removed_in": "python-registry PR",
      "evidence": "hardcoded `None` in registry.rs; never populated"
    },
    {
      "symbol": "`CanonicalType::Date`/`Time` variants",
      "verify": "`rust-packages/laterite-ags4-types/src/lib.rs::CanonicalType::Time`",
      "axis": "code",
      "status": "keep",
      "removed_in": "— (owner call)",
      "evidence": "unreachable via `canonical_type()` but public + matched across 3 bindings — breaking to remove"
    },
    {
      "symbol": "`laterite-ags4-parse` `serde` feature",
      "verify": "`rust-packages/laterite-ags4-parse/Cargo.toml::serde`",
      "axis": "dependency",
      "status": "removed",
      "removed_in": "ags5-relics PR",
      "evidence": "dead #168 scaffold, 0 activators anywhere; dispense-with-ags5 sweep (#177 concept)"
    },
    {
      "symbol": "`CliError` ags5-compat variants",
      "verify": "`rust-packages/laterite-ags4-core/src/error.rs::PreVersion65`",
      "axis": "code",
      "status": "removed",
      "removed_in": "ags5-relics PR",
      "evidence": "`PreVersion65`/`UnknownGroup`/`Predicate`/`UnsupportedFeature`/`Sql`/`Validation` — 0 shipped construction (ags5/ only); `CliError` now `FileNotFound`+`Schema`"
    },
    {
      "symbol": "`Registry::extended_with()`",
      "verify": "`rust-packages/laterite-ags4-core/src/registry.rs::fn extended_with`",
      "axis": "code",
      "status": "removed",
      "removed_in": "ags5-relics PR",
      "evidence": "passthrough-registration; 0 shipped callers (2 in ags5/)"
    },
    {
      "symbol": "`heading_storage_index()`",
      "verify": "`rust-packages/laterite-ags4-core/src/registry.rs::fn heading_storage_index`",
      "axis": "code",
      "status": "removed",
      "removed_in": "ags5-relics PR",
      "evidence": "ags5 parent-overlap-dedup helper; 0 shipped callers (2 in ags5/)"
    },
    {
      "symbol": "`parse.rs` `pub use` block (6 dead names)",
      "verify": "`rust-packages/laterite-ags4-validator/src/parse.rs::parse_bytes_opts`",
      "axis": "code",
      "status": "removed",
      "removed_in": "ags5-relics PR",
      "evidence": "`InvalidUtf8`/`LineSpan`/`LineSpans`/`LineTerminator`/`ParseOptions`/`parse_bytes_opts` re-exported but imported by nobody; live names kept"
    },
    {
      "symbol": "`Dictionary::group_count()`/`heading_count()`",
      "verify": "`rust-packages/laterite-ags4-validator/src/dict.rs::fn group_count`",
      "axis": "code",
      "status": "removed",
      "removed_in": "ags5-relics PR",
      "evidence": "test-only; call-sites inlined to `.groups.len()`/`.headings.len()`"
    },
    {
      "symbol": "`crossterm` (opt dep)",
      "verify": "`rust-packages/laterite-cli/Cargo.toml::crossterm`",
      "axis": "dependency",
      "status": "keep",
      "removed_in": "— (owner call)",
      "evidence": "reached via `ratatui::crossterm`; needed by the `tui` feature gate"
    },
    {
      "symbol": "`DuckParse::surface` field",
      "verify": "`rust-packages/laterite-ags4-compliance/src/bin/duckdb_parse_check.rs::surface: String`",
      "axis": "code",
      "status": "removed",
      "removed_in": "ags5-relics PR",
      "evidence": "`#[allow(dead_code)]`; deserialized, never read"
    },
    {
      "symbol": "`_ordered()` (modality helper)",
      "verify": "`tools/gen_modality.py::def _ordered`",
      "axis": "code",
      "status": "removed",
      "removed_in": "the modality drift-gate PR",
      "evidence": "vulture-surfaced 2026-07-20, verified dead: 0 references repo-wide (module *or* test), superseded by `_columns()`/`_grid()`. Trivial standalone leftover — safe to drop in any pass that touches `gen_modality.py` — dropped 2026-08-04, in the pass its own note nominated"
    },
    {
      "symbol": "`include_warnings: true` (diff)",
      "verify": "`rust-packages/laterite-cli/src/commands/diff.rs::include_warnings: true`",
      "axis": "code",
      "status": "removed",
      "removed_in": "mutation-sweep cleanup (coverage-rust)",
      "evidence": "inert survivor 2026-07-27: `diff` never runs the rule engine, so `opts.include_warnings` was never read — cargo-culted from `validate`/`fix`. Collapsed to `CheckOptions::default()`. See [[mutation-sweep]]"
    },
    {
      "symbol": "`include_warnings: true` (merge)",
      "verify": "`rust-packages/laterite-cli/src/commands/merge.rs::include_warnings: true`",
      "axis": "code",
      "status": "removed",
      "removed_in": "mutation-sweep cleanup (coverage-rust)",
      "evidence": "same inert pattern as `diff` — `merge` parses/reconciles but never runs the rule engine, so `opts.include_warnings` was never read. Collapsed to `CheckOptions::default()`"
    },
    {
      "symbol": "`GroupDescriptor::table()`/`view()`",
      "verify": "`rust-packages/laterite-ags4-reference/src/union.rs::fn table`",
      "axis": "code",
      "status": "removed",
      "removed_in": "#175, re-removed in #189",
      "evidence": "DuckDB `g_<code>`/`v_<code>` name builders on the reference-data leaf. Verified dead 2026-07-29: the only occurrences repo-wide were the two fns and their own unit test, and `laterite-duckdb` never references `GroupDescriptor` — it builds no `g_`/`v_` names at all. Publishing would have frozen an unrelated product's naming into a dictionary crate's public API. Deleted in #175; **#178 restored them** (it branched before #175 and squash-merged after, so its diff undid the removal — the same revert that took out the #176 crate rename). Re-deleted in #189. See [[dec-rust-api-crates-io]], laterite#161"
    },
    {
      "symbol": "`_template-requirement.md` — the `requirement` page class",
      "verify": "`ags-wiki/templates/_template-requirement.md::AGS5 req`",
      "axis": "doc",
      "status": "spotted",
      "removed_in": "not yet — #500, which retires the AGS5 page classes from the public vault",
      "evidence": "AGS5-strand page class with **0 live pages** (`type: requirement` outside `templates/`: 0). Not a plain delete: `lint.py` builds `TEMPLATE_SCHEMA` *from* each `_template-<class>.md`, so removing the file removes the schema for its `type:` — a vault-wide change, not a file removal. The AGS5 strand is dormant and lives in the private satellite, so its page classes are residue here. Spotted 2026-08-12 while removing the AGS5 title from `_template-decision.md`; templates are invisible to the linter (`SKIP_DIRS` includes `templates`, deliberately — they are its schema source), which is why this survived. See [[dec-monorepo-structure]]"
    },
    {
      "symbol": "`_template-experiment.md` — the `experiment` page class",
      "verify": "`ags-wiki/templates/_template-experiment.md::AGS5 experiment`",
      "axis": "doc",
      "status": "spotted",
      "removed_in": "not yet — #500, which retires the AGS5 page classes from the public vault",
      "evidence": "Same shape as the `requirement` class above: **0 live pages** (`type: experiment` outside `templates/`: 0), AGS5-worded throughout (its `evidence` field still points into the decoupled `packages/ags5-` tree), and load-bearing as a `TEMPLATE_SCHEMA` source until the class itself is retired. Retire both together or neither. Spotted 2026-08-12"
    },
    {
      "symbol": "`list_rules()` (wasm)",
      "verify": "`rust-packages/laterite-ags4-wasm/src/metadata.rs::pub fn list_rules`",
      "axis": "code",
      "status": "keep",
      "removed_in": "— (published surface)",
      "evidence": "Inventoried by #349 as having **no web caller** (0 hits in `web/src`, `web/e2e`, `web/scripts` — verified 2026-08-18; the Rule explainer reads `public/rules-catalogue.json`, synced by `scripts/sync-rules.mjs`). Not a relic: it is part of the **published** `@laterite/ags4-wasm` surface, named in `EXPECTED_FUNCTIONS` in BOTH `tools/release/check-wasm-slim.mjs` and `check-wasm-tier1.mjs`, documented available in `web/docs-site/docs/reference/wasm-api.md`, with a cookbook page and node/py twins. \"No UI caller\" is not \"no caller\" — removing it is an npm breaking change, not a cleanup."
    },
    {
      "symbol": "`version()` (wasm)",
      "verify": "`rust-packages/laterite-ags4-wasm/src/metadata.rs::pub fn version`",
      "axis": "code",
      "status": "keep",
      "removed_in": "— (live caller)",
      "evidence": "Inventoried by #349 as having no web caller — **true, and misleading**: it has a live NON-web caller. The wasm compliance runner calls it (`glue.version?.()`, the satellite's `tools/compliance/emit_js.mjs`), which is the exact purpose its own docstring records — it exists because that harness had hard-coded `version: \"0.5.1\"` and printed a false cross-surface identity (laterite-dev#556). Also in both release gates' `EXPECTED_FUNCTIONS` and the published API docs. Deleting it would re-open the bug it was added to close."
    },
    {
      "symbol": "`engine_version()` (wasm)",
      "verify": "`rust-packages/laterite-ags4-wasm/src/metadata.rs::pub fn engine_version`",
      "axis": "code",
      "status": "keep",
      "removed_in": "— (cert-trust, prospective)",
      "evidence": "No web caller (verified 2026-08-18). Retained on two independent grounds. (1) **Prospective feature, not dead code**: [[cert-trust-v2]] defines engine identity as a tuple `EngineId = (validator, validator_version, engine_fingerprint, compat)`, and this export is the browser's route to the validator's own version — a browser-minted certificate needs it. (2) It is in `EXPECTED_FUNCTIONS` in both release gates and the published API docs. Its docstring is candid that it is \"useful for humans, useless as an identity\" on its own — the identity claim belongs to the fingerprint below; the two are recorded together because a future cert-trust build reads them as a pair."
    },
    {
      "symbol": "`engine_fingerprint()` (wasm)",
      "verify": "`rust-packages/laterite-ags4-wasm/src/metadata.rs::pub fn engine_fingerprint`",
      "axis": "code",
      "status": "keep",
      "removed_in": "— (cert-trust, prospective)",
      "evidence": "No web caller (verified 2026-08-18). **A missing feature, not a dead one** — the case #349 asked to be checked before removal, and the check says keep. [[cert-trust-v2]] names `engine_fingerprint` in `EngineId` and requires its equality for a cert to be trusted; it is a build-time SHA-256 over every rule source, the dictionary and the rules catalogue, so it is the only thing that can show two surfaces agree on the RULES rather than merely shipping together. Deleting it would have to be walked back the moment browser-minted certs land. Also in both release gates' `EXPECTED_FUNCTIONS`, the published API docs, and `tools/xcheck/`."
    },
    {
      "symbol": "`build_ags4_ipc()` (wasm)",
      "verify": "`rust-packages/laterite-ags4-wasm/src/build.rs::pub fn build_ags4_ipc`",
      "axis": "code",
      "status": "keep",
      "removed_in": "— (published, feature-gated)",
      "evidence": "Inventoried by #349 as never imported by either worker — true (the app's Export pane uses the JSON door `build_ags4`). Not a relic: it is the **Arrow** door of the same verb, gated behind the `arrow` feature, documented in `wasm-api.md` as \"build from source\" and in `surfaces/browser.md`, and carried in the cross-surface modality register (`modality.json`, held by `packages/laterite/tests/test_modality_parity.py`) — so removing it would drop a modality row and fail that gate. `check-wasm-tier1.mjs` names it among the exports whose ABSENCE defines tier 1, which is a dependency on the name too."
    },
    {
      "symbol": "`dictionary()` (wasm)",
      "verify": "`rust-packages/laterite-ags4-wasm/src/dictionary.rs::pub fn dictionary`",
      "axis": "code",
      "status": "keep",
      "removed_in": "— (published surface)",
      "evidence": "Made caller-less on the web by **this** change (#349): the app's Dictionary browser and Template generator read the static union `ags_dictionary.json` via `lib/dict.ts`, which #349 settled as the web's single dict source, so the worker op and client fn were removed. Recorded here per the relic discipline — add the row in the PR that strands the export — but `keep`, on the same grounds as its four siblings above: it is named in `EXPECTED_FUNCTIONS` in BOTH `tools/release/check-wasm-slim.mjs` and `check-wasm-tier1.mjs` and documented available in `web/docs-site/docs/reference/wasm-api.md`, so it is published npm surface. Its TYPE is still load-bearing web-side even with no caller: `web/src/lib/validator.ts` re-exports `StandardDict` from the engine so the local projection must keep conforming to what this export returns."
    },
    {
      "symbol": "`laterite_ags4_validator::is_valid()`",
      "verify": "`rust-packages/laterite-ags4-validator/src/lib.rs::pub fn is_valid`",
      "axis": "code",
      "status": "spotted",
      "removed_in": "the release that bumps the engine tier off 0.9.0",
      "evidence": "Renamed to `is_clean` in #321, when a warning stopped failing a file and \"zero findings\" stopped being the verdict — two public `is_valid` in one crate that disagree on a warning-carrying file. Kept as a `#[deprecated]` delegating alias so the rename reaches a crates.io consumer as a warning naming both replacements rather than a build failure; 0 callers in this tree."
    },
    {
      "symbol": "`.github/workflows/allocator-bench.yml` + `tools/bench/allocator_ab.py`",
      "verify": "`.github/workflows/allocator-bench.yml::allocator-bench (temporary, #448)`",
      "axis": "code",
      "status": "removed",
      "removed_in": "the PR that recorded the #448 measurement (runs 32418268428 / 32421189772)",
      "evidence": "Added deliberately temporary and inventoried in the same change; removed in the PR its own row named. It was an INSTRUMENT, not a gate — `workflow_dispatch`-only, never on a push, a PR or a schedule — and it answered exactly one question: on Linux and Windows, does mimalloc v3 beat the v2 pin by enough to narrow that pin to `cfg(target_os = \"macos\")`? Measured on both self-hosted runners at the 25 MB rung: no. The system-allocator control separated by 36.2% on Linux and 62.6% on Windows, so the harness demonstrably resolved allocator effects — and against that, the v2/v3 gap sat inside the noise floor on both, pointing in OPPOSITE directions (v3 3.3% slower on Linux, 1.7% faster on Windows). The answer is on #448; the harness that produced it has nothing left to measure. Kept past that point it would be a build cost nobody could justify and nobody would dare delete."
    },
    {
      "symbol": "`tests/p1-flagship.cjs` (node)",
      "verify": "rust-packages/laterite-node/tests/p1-flagship.cjs::P1 — the native engine core",
      "axis": "code",
      "status": "spotted",
      "removed_in": "not yet — the PR that next touches the node surface's test wiring",
      "evidence": "A pre-vitest CommonJS smoke script, sole occupant of `tests/` — and `vitest.config.ts` reads `test/`, one letter away, which is why nothing has run it and nothing noticed. Zero references repo-wide (no `package.json` script, no workflow, no doc); it arrived in a pre-flip `sync: public tree` commit and no commit has touched it since. What it asserts — parse→Arrow IPC, validate, byte-faithful emit, data→AGS4 — is covered by the `test/` suite that replaced it, so this is a superseded copy rather than lost coverage. Found by `knip` (the only genuine orphan in the 2026-08-21 three-scout pass; vulture's four `tools/` hits all had live callers in `tests/`, and web's 70 knip hits are overwhelmingly the #418 Solid primitives, built ahead of the app restyle they are for). **Confirm before deleting**: a file vitest cannot see is exactly the shape that reads as dead while being someone's manual smoke test."
    }
  ]
}
