{
  "schema": 1,
  "cases": [
    {
      "id": "validate.json.findings",
      "op": "validate_json",
      "input": { "fixture": "rust-packages/laterite-ags4-validator/tests/fixtures/rule8_dp_wrong_precision.ags" },
      "legs": ["cli-native", "cli-uvx", "cli-npx"],
      "spec_ref": "`lat validate --json` renders findings through a HAND-SYNCED renderer triplicated across the three launchers (laterite-py lib.rs, laterite-node lib.rs 'ported verbatim', render.rs 'byte-identical'). Three hand-synced copies held by discipline is the exact shape that produced drifts #1 and #2 — so the whole render (findings map + exit code) must be byte-identical."
    },
    {
      "id": "validate.json.clean",
      "op": "validate_json",
      "input": { "fixture": "rust-packages/laterite-ags4-validator/tests/fixtures/clean_minimal.ags" },
      "legs": ["cli-native", "cli-uvx", "cli-npx"],
      "spec_ref": "The empty-findings render path — a clean file exits 0 with an empty findings map, identically across launchers."
    },
    {
      "id": "validate.json.warning_tier",
      "op": "validate_json",
      "input": { "fixture": "rust-packages/laterite-ags4-xcheck/cases/inputs/warning_tier_only.ags" },
      "legs": ["cli-native", "cli-uvx", "cli-npx"],
      "spec_ref": "A file whose ONLY finding is a WARNING passes (#321): the report shows it, the verdict ignores it, exit 0. The fixture is deliberately warning-pure — an unrecognised TRAN_AGS over an otherwise clean file — so the exit code turns on the tier split and nothing else. Each launcher wires the verdict by hand (render.rs, _cli.py, cli.ts), so nothing but a cross-launcher gate catches one of the three still failing on warnings."
    },
    {
      "id": "validate.json.warning_tier.werror",
      "op": "validate_json",
      "input": {
        "fixture": "rust-packages/laterite-ags4-xcheck/cases/inputs/warning_tier_only.ags",
        "flags": ["--warnings-as-errors"]
      },
      "legs": ["cli-native", "cli-uvx", "cli-npx"],
      "spec_ref": "The SAME file under `--warnings-as-errors` fails on all three launchers — exit 1, identical render. The pair is the point: one file, one flag, opposite verdicts, so a launcher that ignores the flag (an unwired opt-in reads as a silent pass) splits against the other two instead of quietly agreeing with the default."
    },
    {
      "id": "validate.ndjson.findings",
      "op": "validate_ndjson",
      "input": { "fixture": "rust-packages/laterite-ags4-validator/tests/fixtures/rule8_dp_wrong_precision.ags" },
      "legs": ["cli-native", "cli-uvx", "cli-npx"],
      "spec_ref": "`lat validate --ndjson` over the same findings fixture as the `--json` case — one flat JSON object per finding per line. `--ndjson` is the ONLY output flag validate honours that no other verb does (a global flag every verb accepts but only validate reads; a silent no-op on diff/read/merge/rules), and it is a SEPARATE renderer (`findings_ndjson`) hand-synced across the three surfaces — so it is pinned in its own right, not assumed covered by `--json`."
    }
  ]
}
